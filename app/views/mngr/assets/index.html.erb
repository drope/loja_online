<h1>Listing assets</h1>

<table>
  <tr>
	<td></th>
	<td></th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @assets.each do |asset| %>
  <tr>
	<td><%= asset.name %>
	<td><%= image_tag asset.photo.url(:thumb) %></td>
    <td><%= link_to 'Show', [:mngr, asset] %></td>
    <td><%= link_to 'Edit', edit_mngr_asset_path(asset) %></td>
    <td><%= link_to 'Destroy', [:mngr, asset], :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Asset', new_mngr_asset_path %>
